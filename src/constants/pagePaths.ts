const enum PagePaths {
  root = '/',
  signIn = '/sign-in',
  signUp = '/sign-up',
  boards = '/boards',
  tasks = '/tasks',
  addTask = 'add-task',
  addBoard = 'add',
  dynamicParam = 'id',
}

export default PagePaths;
