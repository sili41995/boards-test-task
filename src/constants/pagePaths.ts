const enum PagePaths {
  root = '/',
  signIn = '/sign-in',
  signUp = '/sign-up',
  boards = '/boards',
  tasks = '/tasks',
  addTask = 'add-task',
  dynamicParam = 'id',
}

export default PagePaths;
